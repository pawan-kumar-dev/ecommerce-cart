import React,{memo} from 'react'
const Header=()=>{
    return(
        <header>
            <nav>
                <h2>React Based Shopping Cart App</h2>
            </nav>
        </header>
    )
}
export default memo(Header);